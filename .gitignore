# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
dist/
build/

# Node
node_modules/
.vite/
web/dist/
web/dist-types/
web/*.tsbuildinfo

# Editors / OS
.DS_Store
.idea/
.vscode/
*.swp

# Local config
.env
.env.*
!.env.example

# Local candidate artifacts (real resume PDFs etc.) — keep curated examples.
/*.pdf

# Generated template previews (regenerate with `make previews`).
templates/*/preview.png

# Claude Code session state (per-session, not per-repo).
.claude/.session-branch
